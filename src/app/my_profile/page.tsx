"use client"; // Asegúrate de incluir esto si MyProfile es un componente cliente

import MyProfile from "@/components/MyProfile"; // Ajusta la ruta según tu estructura

export default function MyProfilePage() {
  return <MyProfile />;
}
